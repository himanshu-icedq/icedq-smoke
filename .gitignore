node_modules/
out/
hars/
*.har
.env
.env.*
!.env.example
*.pem
*.key
playwright-report/
test-results/
.DS_Store
.idea/
*.jfr

# Real Replicated license pulled from a live cluster — a production
# credential, never commit it.
local-deps/replicated-config.yaml

# Runtime artifacts generated by dev.sh against a live compose stack —
# regenerated on every `./dev.sh up`, not something to commit.
local-deps/generated/
local-deps/.dev-run/
